- hosts : all
  remote_user : ec2-user
  become : yes
  tasks :
   - name : apache
     yum : 
      name: httpd
      state: present
   - name : servive start
     service:
      name: httpd
      state: started
   - name : copy file
     copy:
      src: /root/index.html
      dest: /var/www/html/
